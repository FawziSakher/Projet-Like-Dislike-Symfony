{% extends 'base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
    <h1 style="color: red; text-align: center;">Post</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ post.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ post.content }}</td>
            </tr>
        </tbody>
    </table>

    &nbsp;&nbsp;&nbsp;<a href="{{ path('app_post_index') }}">Retour</a>

    <span style="margin-left: 20px;"></spam>

    <a href="{{ path('app_post_edit', {'id': post.id}) }}">Modifier</a>

    {{ include('post/_delete_form.html.twig') }}
{% endblock %}
